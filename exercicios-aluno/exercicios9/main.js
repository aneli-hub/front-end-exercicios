//Escreva um programa em que o usuário insira 10 números, exiba a contagem de quantos números inseridos são maiores do que 50;

let numeros = 0;
let soma;

for (let contador = 0; contador < 10; contador = contador + 1 ) {
    soma = prompt("digite 10 numeros");
    soma = Number (soma);
    numeros = numeros + soma;
    
//falta o if

} console.log('soma dos numeros maiores de 50 ', ());
